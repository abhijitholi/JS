const a = 3;
const b = -2;
// console.log(a > 2);
// console.log(b > 0);

// console.log(a > 2 && b > 0);
console.log(true && true);
// This line uses the && (logical AND) operator to check if both true conditions are satisfied. Since both operands are true, the result will be true. The output of this line will be: 
console.log(true && false);
// Here, the && operator is used to check if both true and false conditions are satisfied. However, since one of the operands is false, the overall result will be false. The output will be:
console.log(false && true);
console.log(false && false);
// always 


// Expected output: false
